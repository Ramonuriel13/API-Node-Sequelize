const express = require('express');
const app = express();
const db = require('./app/models');

const SeedFunction = () => {

    //produtos.create({ name: 'calção', value: 60.00, amount: 20 });
    //res.produtos.findAll().then(result=> console.log(result))
    //produtos.create({name: 'camisa da roma', value:'180.00', amount:'30'});
    //db.produtos.create({name: 'bola da Pl', value:'100.00', amount:'40'});
    // produtos.create({name: 'meião', value:'25.00', amount:'20'});

}

module.exports = SeedFunction;